/**
 * I only used this file if there is a need to share something between the backend and the frontend
 */

export const api = "api/tag";

export const event = {
  insert: "valiu-insert",
  update: "valiu-update",
  delete: "valiu-delete"
};
