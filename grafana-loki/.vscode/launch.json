{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "name": "Run Script: start",
            "request": "launch",
            "command": "yarn run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}